const words = [
	"cars"
	"paper"
	"book"
	"name"
	"crazy"
	"dino"
	"bugs"
	"tarp"
	"kill"
	"make"
	"water"
	"bean"
]

export default do
	const index = Math.floor(Math.random! * words.length)
	const word = words[index].toUpperCase!
	word
